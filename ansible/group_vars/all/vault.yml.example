---
# Ansible Vault Configuration Example
# ====================================
# This file shows ALL possible vault variables for reference.
# 
# IMPORTANT: When using Pilvio S3 with automatic setup:
# - S3 credentials are AUTO-GENERATED during terraform apply
# - Only create a vault.yml for ADDITIONAL secrets not covered by automation
# - If you need to add manual secrets, merge them with the auto-generated vault.yml

# ============================================================================
# AUTO-GENERATED VARIABLES (Created by Terraform)
# ============================================================================
# These are automatically created in vault.yml during 'terraform apply':
#
# S3 Credentials:
# vault_s3_access_key: "auto-generated"
# vault_s3_secret_key: "auto-generated"
# vault_s3_endpoint: "https://s3.pilw.io"
# vault_s3_bucket_backup: "from-terraform-vars"
# vault_nextcloud_s3_bucket: "from-terraform-vars"
#
# Traefik Dashboard:
# vault_traefik_dashboard_user: "admin"
# vault_traefik_dashboard_password: "auto-generated-htpasswd-hash"
#
# Nextcloud Database:
# vault_nextcloud_db_password: "auto-generated"
# vault_nextcloud_db_root_password: "auto-generated"
#
# Nextcloud Admin:
# vault_nextcloud_admin_user: "admin"
# vault_nextcloud_admin_password: "auto-generated"
#
# DO NOT manually add these! They are automatically generated for security.

# ============================================================================
# OPTIONAL MANUAL CONFIGURATION
# ============================================================================
# Only add these if you need to override defaults or add extra features:

# Slack webhook for monitoring alerts (optional)
vault_slack_webhook_url: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL"

# Mailcow admin password (optional - defaults to 'moohoo', change via UI)
vault_mailcow_admin_password: "secure-password-here"

# SMTP Password for Nextcloud (create mailbox in Mailcow first)
vault_nextcloud_smtp_user: "noreply@yourdomain.com"
vault_nextcloud_smtp_password: "get-from-mailcow-after-creating-mailbox"

# ============================================================================
# HOW TO USE THIS FILE
# ============================================================================
# 
# Option 1: Let Terraform handle everything (RECOMMENDED)
# -------------------------------------------------------
# 1. Don't create vault.yml manually
# 2. Run 'terraform apply' - it creates vault.yml with:
#    - S3 credentials
#    - Traefik dashboard credentials
#    - Nextcloud database passwords
#    - Nextcloud admin credentials
# 3. Check terraform output for initial passwords
# 4. Only add manual secrets if needed later
#
# Option 2: Add additional secrets to auto-generated vault
# -------------------------------------------------------
# 1. Run 'terraform apply' first (creates vault.yml with S3 creds)
# 2. Decrypt the auto-generated vault:
#    ansible-vault decrypt ansible/group_vars/all/vault.yml
# 3. Add your additional secrets
# 4. Re-encrypt:
#    ansible-vault encrypt ansible/group_vars/all/vault.yml
#
# Option 3: Manual setup (only for non-Pilvio S3)
# -------------------------------------------------------
# 1. Copy this file to vault.yml
# 2. Fill in ALL required values including S3 credentials
# 3. Encrypt with: ansible-vault encrypt vault.yml